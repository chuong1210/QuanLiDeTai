import { Loader } from "@/resources/components/UI";
import React from "react";

const Loading = async () => {
  return (
    <div>
      <Loader />
      Loading
    </div>
  );
};

export default Loading;
