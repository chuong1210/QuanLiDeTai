export default function Page({ params: { id_task } }: { params: { id_task: string } }) {
    return <div>
        trong đây hiện chi tiết thông tin cái nhiệm vụ giáo viên giao {id_task}
    </div>

}
